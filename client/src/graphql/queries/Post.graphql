query Post ($id: Int!) {
    post (id: $id) {
        title
        text
        points
        createdAt
        creator {
            username
            posts {
                title
            }
        }
    }
}